# Caculator
## 📝 Ultra-Concise README  This command-line **Simple Python Calculator** handles addition, subtraction, multiplication, and division. It uses separate functions for each operation and features an interactive, looping menu. Key strengths include **error handling** for division by zero and invalid input. Run it by executing `python temp.py`.
